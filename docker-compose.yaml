services:
  postgres:
    image: mcr.microsoft.com/mssql/server:2022-latest
    env_file: .env
    ports:
      - "1433:1433"
    volumes:
      - mssql_data:/var/opt/mssql
volumes:
  mssql_data:
    driver: local